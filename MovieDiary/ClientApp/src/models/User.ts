export type User = {};

export type LoginFormValues = {
  username: string;
  password: string;
};
export type RegisterFormValues = {
  username: string;
  name: string;
  email: string;
  password: string;
};
