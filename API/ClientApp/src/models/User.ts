export type User = {
  id: string;
  name: string;
  userName: string;
  token: string;
};

export type LoginFormValues = {
  username: string;
  password: string;
};
export type RegisterFormValues = {
  username: string;
  name: string;
  email: string;
  password: string;
};
